<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.mobven.uicomponents.amount.AmountInputView
        android:id="@+id/amountInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:box_background_color="@color/white"
        app:box_stroke_color="@color/limed_spruce"
        app:edit_hint_text="Amount"
        app:edit_hint_text_color="@color/silver_sand"
        app:edit_text_appearance="@style/MyValueTextStyle"
        app:input_hint_text="Amount"
        app:input_hint_text_appearance="@style/MyKeyTextStyle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>